# Phorrom sidecar — Phase 1 dependencies (pinned to Python 3.11).
# Heavy ML deps (torch, onnxruntime, chromadb, sentence-transformers) are added in later
# phases to keep the Phase-1 footprint small and the install fast.
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
python-dotenv>=1.0

# --- orchestrator (Phase 3) ---
networkx>=3.0          # subtask DAG + scheduling
pulp>=2.7,<4.0         # token-budget assignment optimization (bundled CBC solver)

# --- ML estimators (Phase 4) ---
scikit-learn>=1.3      # token/quality regressors (small-data friendly)
joblib>=1.3            # model persistence

# --- documents (Phase 4 / #5) ---
jinja2>=3.1            # report templating (rendered to md/docx/pdf via external Pandoc)

# --- cloud backup (Google Drive) ---
google-auth>=2.0
google-auth-oauthlib>=1.0   # installed-app loopback OAuth
google-api-python-client>=2.0
cryptography>=42.0          # client-side backup encryption (PBKDF2 + Fernet)
# Native tools used by docs/multimodal are NOT pip deps: Pandoc, TinyTeX (LaTeX),
# Tesseract, whisper.cpp — discovered at runtime (see sidecar/tools.py). PyInstaller is a
# build-time tool for packaging the sidecar (not a runtime dependency).

# --- dev / test ---
pytest>=8.2
pytest-asyncio>=0.23
anyio>=4.4
